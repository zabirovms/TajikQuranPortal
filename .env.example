# Database Configuration
# Replace with your Supabase or PostgreSQL connection string
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.example.supabase.co:6543/postgres

# Node Environment
NODE_ENV=development

# Optional: Set port for the server (defaults to 5000 if not set)
PORT=5000